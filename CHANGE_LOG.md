# Change Log

Append-only, reverse-chronological. One entry per discrete change.
Format: `YYYY-MM-DD | scope | summary`

---

## 2026-05-04

- 2026-05-04 | step-10-pr37 | **PR 37 — SkillResolver `synthesize` mode + `source_class` in resolver payload.** Two changes that landed together because they reinforce each other. **(1) Source metadata in the LLM prompt:** `_format_skill_user` (`skills/llm_client.py`) now includes `source_class` (e.g. `gmail`, `gong-call`, `slack`) and `source_label` per candidate, populated by `core/read.py` via a single `DocumentRow` SELECT before each `resolve()` call. New `SkillResolverSourceMeta` frozen dataclass exported from `llm_client.py`. Without this, prompts like "written outweighs verbal" only worked because the demo's `agent_id` happened to encode modality; now the LLM has a first-class signal regardless of agent naming. **(2) Synthesize mode:** `SkillResolverSpec` gains `synthesize: bool = False`. In `pick` mode (default, prior contract) the LLM returns one candidate's `value_json` byte-for-byte; in `synthesize` mode the LLM produces a NEW value derived from the candidates (summary, normalised form, aggregate). `ResolvedFieldValue` gains `synthesized_value_json` + `synthesized_confidence` — when set, `winner` is None and `core/read.py::_to_field_value` builds a KNOWN `FieldValue` whose `value` is the synthesised payload and whose `lineage` attributes EVERY candidate as a contributor (no single anchor row). Refusals (LLM returns `chosen_value_json=null`) still map to UNRESOLVED with all candidates surfaced — never silent concatenation, in either mode. **SKILL.md** rewritten with a `MODE: pick|synthesize` line plus a third worked example for synthesise. **UI:** resolver editor shows a "synthesize ✨" checkbox below the prompt textarea when type=skill; the lineage flow detects synthesised winners (KNOWN + no candidate value matches the resolved value) and renders all curves at full opacity with a "synthesize ✨ skill resolver" label and a `N → 1 synthesised value` detail line. **Tests:** two new cases — synthesise-mode happy path lands KNOWN with `synthesized_value_json` and no `winner`; synthesise-mode refusal lands UNRESOLVED with all candidates intact. All five existing test fakes (`_helpers.py`, `resolve_test.py`, `nl_to_ruleset_test.py`, `ingestor_test.py`) widened to accept the new `mode` and `source_metadata` kwargs. **193/193 unit tests pass; ruff + format clean.** Resolves the user-reported confusion where "Summarise them all" returned an UNRESOLVED reason; that prompt now works in synthesise mode and remains UNRESOLVED (not concatenated) in pick mode.
- 2026-05-04 | step-10-pr36-followup | **PR 36 follow-up — `prefer_agent` resolver type fully removed (Python + JS).** The demo's resolver editor exposes only the three user-facing types (`latest_write` / `skill` / `auto`); leaving `prefer_agent` supported in the backend but absent from the editor was creating a control-plane gap (operators couldn't tune `agent_id` from UI without overwriting the policy) flagged by adversarial review. Decision: drop `prefer_agent` rather than re-add it to the editor — it was an early scaffolding type, not part of the demo's narrative (which is "latest write vs LLM-driven skill resolution"). **SDK (`packages/kentro/src/kentro/`):** `PreferAgentResolverSpec` removed from `types.py` + `__all__`; class `PreferAgent` removed from `resolvers.py` + `__all__` + `__init__.py` re-export; render branch removed from `rules.py::render_resolver_policy`; `client.py::read_with` docstring updated. **Server (`packages/kentro_server/src/kentro_server/`):** `core/resolve.py` drops the `PreferAgentResolverSpec` import + `isinstance` branch + `_PREFER_AGENT_NO_MATCH` constant; `api/routes/entities.py::read_with` docstring updated. **UI (`packages/kentro_server/src/kentro_server/static/panels.jsx`):** comment update — the legacy-resolver banner now refers to `raw` only (since that's the only remaining backend type the editor doesn't surface). **Tests:** removed `test_prefer_agent_picks_matching_when_match_exists` + `test_prefer_agent_no_match_returns_unresolved` from `tests/unit/resolve_test.py`; removed `test_render_resolver_policy_prefer_agent` from `tests/unit/rules_helpers_test.py`. Doc updates: `implementation-handoff.md` (resolvers list, Step 5), `memory.md` (built-in resolvers list). **191/191 unit tests pass; ruff + format + ty clean.**
- 2026-05-04 | step-10-pr36 | **PR 36 — Resolver editor moved out of the lineage drawer into its own stacked overlay.** PR 35 hosted the resolver editor inline under the RESOLVER chip in `LineageDrawer`, but the chip's column has no fixed width, so the editor (`width: 100%` of that column) escaped the drawer to the right (clipping the apply button), painted on top of the candidate value chips and SVG connectors in the middle of the flow, and broke the geometry of the connectors it was meant to help reason about. New `K.ResolverDrawer` (`panels.jsx`) is a third stacked left-of-right-rail panel: 380px wide, pinned at `right: 960px` so its right edge meets the lineage drawer's left edge. Same `.drawer` chrome as `LineageDrawer`/`PolicyOverlay`, plus a new `.drawer-deep` modifier in `styles.css`. The form body (`ResolverEditorForm`) is the same 5-option select + conditional `agent_id` / `prompt` inputs from PR 35, now hosted as drawer-body content (drawer-head supplies the title + close `esc` button). `LineageFlowLayout` no longer owns `editorOpen` state or renders an inline editor — the RESOLVER chip's `onClick` calls `onEditResolver({entity_type, field_name})` upward; `LineageFlow`/`LineageDrawer` pass it through to `app.jsx`. `app.jsx` adds `resolverTarget` state, an `openResolver` callback wired to `LineageDrawer.onEditResolver`, and a `lineageRefreshKey` counter bumped on `ResolverDrawer.onApplied` so `LineageDrawer`'s read effect (now keyed on `refreshKey`) re-fetches and the flow re-renders the new winner. Old `.resolver-editor*` outer-card CSS (border, shadow, head, close button) deleted; `.resolver-editor-row` / `.resolver-editor-actions` / `.resolver-editor-error` rules retained since the form uses them inside the drawer body. **Two follow-up fixes after first-pass review:** (a) Stale form state when the lineage target switches between fields with different saved resolvers — `ResolverEditorForm` was the same React instance across target changes and its `useState` retained the previous field's resolver type / agent_id / prompt; if the new field had no policy, the early-return in the fetch effect (`if (!policy) return;`) left state stale. Fixed with a `key={entity_type}.${field_name}}` on the form so React unmounts/remounts on target change, plus a defensive defaults-reset at the top of the fetch effect. (b) ESC closed the lineage drawer but left the (deeper) resolver drawer orphaned — both registered capture-phase `keydown` listeners on `window`, fired in registration order, lineage was registered first and called `stopImmediatePropagation`, so the resolver's listener never ran. Fixed by having lineage's handler bail (without stopping) when `.drawer.drawer-deep.open` is in the DOM; resolver's later-registered handler then runs and closes itself. Also added a cascade close in `app.jsx::closeLineage` so closing lineage by any path (ESC button, backdrop click) dismisses the resolver drawer too. **End-to-end verified via chrome-devtools MCP:** click `Customer · Acme Corp` → click `deal_size` → click RESOLVER chip → resolver drawer slides in left of lineage drawer; switching type to `prefer_agent` reveals the agent_id input; setting `sales` + apply bumps rule version, closes the drawer, re-fetches lineage; re-opening pre-populates with saved `prefer_agent` + `sales`; switching the lineage drawer to `name` (no saved policy) and re-opening the resolver chip shows defaults (`latest_write`, no agent_id), not the carried-over `prefer_agent + sales`; first ESC closes resolver drawer only, second ESC closes lineage drawer; entity overlay stays. No console errors. **194/194 unit tests pass; ruff + format clean.**
- 2026-05-04 | step-10-pr35 | **PR 35 — Split resolvers from ACL: separate SDK type, separate DB table, per-type JSON-array rows, no field wildcards, UPSERT semantics.** Resolvers and ACL rules were tangled together as `Rule` variants, which made the data model confusing (a resolver isn't an "access" decision) and the edit semantics painful (the old client-side merge did `[...current, ...parsed]`, so a sequence "allow A read C.d" → "deny A read C.d" → "allow A read C.d" piled up three rows that deny-overrides masked). **Backend (commit 1):** New SDK types `ResolverSpec`, `ResolverPolicy(entity_type, field_name, resolver)`, `ResolverPolicySet(policies, version)` in `kentro.types`; `ConflictRule` deleted. `WriteRule.field_name: str` is now required (no field-level wildcards) — instead, the NL parser fans out "all fields" intents into N per-field rules at parse time. `EntityVisibilityRule.entity_key` stays optional (entity-instance-level wildcard for "agent sees this type at all"). New DB tables `entity_type_rules` and `entity_type_resolvers`, one row per (rule_version, entity_type) holding a JSON array of rules / policies (atomic per-type editing). New `core/resolvers.py` with `apply_resolver_policies()` and `load_active_resolver_policies()`; both `apply_ruleset` and `apply_resolver_policies` forward-port the OTHER table's rows when bumping version (each version is a complete snapshot of (rules, resolvers)). `apply_ruleset` uses UPSERT semantics keyed by `_rule_key(rule)` so the most recent edit wins by (kind, agent, type, field|key). New routes `GET /resolvers/active`, `POST /resolvers/apply`, `GET /resolvers/active/rendered`. `core/resolve.py::resolve()` and `core/read.py::read_entity()` now take `resolver_policies` instead of looking up `ConflictRule`s in the ruleset. NL parser SKILL.md rewritten to describe only the 3 ACL Rule shapes (with snake_case `type` discriminator) and tell the LLM to refuse resolver intents. `skills/llm_client.py::ParsedRule` renamed to `ParsedRules` with `rule_jsons: tuple[str, ...]` (was `rule_json: str | None`) — supports fan-out. Alembic revision `c1a83f240d92_split_resolvers_per_type_rows_no_wildcards` walks `rule_record`, splits `conflict` rows into `entity_type_resolvers`, expands wildcard `write` rows by parsing `schema_type.definition_json`, drops `rule_record`. `demo/ruleset.py` now emits explicit per-field WriteRules via `_expand_write_rules`; new `initial_demo_resolvers()` returns `Customer.deal_size` latest_write `ResolverPolicy`. **UI (commit 2):** `api.js` adds `getResolvers()` and `applyResolvers(policies, summary)`. `PolicyOverlay`'s onApply now sends only the parsed rules (server UPSERTs); the old client-side append-merge is gone. `LineageDrawer + LineageFlow`: the RESOLVER chip is clickable when (entity_type, field_name) is known — opens an inline `ResolverEditor` with a type dropdown (latest_write / prefer_agent / skill / raw / auto) plus type-specific fields (`agent_id` for prefer_agent, `prompt` for skill); applies via `/resolvers/apply` and re-fetches the entity so the flow re-renders with the new winner. `app.jsx` drops `conflictPolicy` state and the topbar conflict-policy chip — that chip used to read `ConflictRule` rows that no longer exist. New CSS for `.resolver-editor` and `.is-clickable` RESOLVER chip hover. **Sanity (post-seed DB dump):** `entity_type_rules` 5 rows × 2 versions (Customer, Person, Deal, Note, AuditLog); `entity_type_resolvers` 1 row at v2 (Customer.deal_size latest_write); end-to-end resolver edit verified: POST `/resolvers/apply {Customer.deal_size → prefer_agent agent_id=sales}` bumped v2→v3, all 5 ACL rule rows forward-ported. **194/194 unit tests pass; ruff + format + ty clean.**
- 2026-05-04 | step-10-pr34 | **PR 34 — PolicyOverlay + AgentPanel removal + permanent right rail + NL parser fixes.** Retired the top-row `<AgentPanel>` (Sales / CS) + standalone `<PolicyEditor>` + standalone `<AccessMatrixPanel>` from the main grid; their data is now reachable via the EntityOverlay → LineageDrawer / PolicyOverlay flow. Main canvas is now graph-only (`.main-graph-only`). New `K.PolicyOverlay`: type-scoped right-edge drawer opened from a new `[ACL]` chip in EntityOverlay's header. Stacks left of the entity rail (mutex with LineageDrawer). Three sections: type-scoped Access matrix (admin agents filtered out — they bypass ACL), filtered rule list (only Customer/etc. rules), NL editor with type-aware suggestion chips. **Permanent right rail:** the 440px right side is always reserved (CSS grid `1fr 440px` on `.main-graph-only`); SourceOverlay / EntityOverlay no longer slide-in, content swaps in place when the user clicks a different entity. New `K.EmptyRightRail` placeholder when nothing is selected. Drawer always positions at `right: 440px` to clear the rail. Backdrop dimming retired. **NL rule parser fixed:** `nl_to_rule/SKILL.md` was telling the LLM to produce a Rule using class names like `FieldReadRule`, but the discriminated-union uses `type` with snake-case literals (`field_read`, `entity_visibility`, `write`, `conflict`). Updated the prompt with exact JSON shapes for every Rule + ResolverSpec variant, including the required `prompt` field on `skill` resolver, plus three worked examples. `nl_to_ruleset.py` now surfaces the actual Pydantic validation error path + raw LLM JSON in the skip-note instead of an opaque "(N errors)" — turns silent failures into actionable feedback. **`.md` strip on string field values:** `K.fmtFieldValue` applies `K.docLabel` to string values so a Note's `source_label="acme_ticket_162.md"` renders as `acme_ticket_162`. **Suite: 199/199 unit tests pass; ruff + format clean.**
- 2026-05-04 | step-10-pr33 | **PR 33 — UI #2 + #3 (entity overlay, source overlay), lineage flow visualization, admin/ingestion split, admin ACL bypass, per-source lineage values.** Two right-edge slide-overs (`SourceOverlay` for raw doc content; `EntityOverlay` for Global + per-agent cards on a single entity); both share the existing `.source-overlay*` chrome and live at App level so they cover Policy / Access Matrix, not just the panel that triggered them. **Lineage flow** (replaces the kv-list lineage drawer body): SVG-based horizontal pipeline rendering CANDIDATES → RESOLVER → RESULT, each candidate row a colored source-info card with the written value as a chip riding on a per-source-colored bezier arc into the resolver chip; orange winner arc out to the result. After ESC bug report, drawer's keydown listener now runs in capture phase + stopImmediatePropagation so ESC closes the drawer first while EntityOverlay stays open. **Server: admin bypasses ACL on reads.** `read_entity()` accepts `bypass_acl: bool` (routes pass `principal.is_admin`); the Global card now sees every populated field regardless of which FieldReadRules exist for the admin agent. **Agent split:** `tenants.json` adds a separate `admin` agent (is_admin=true, control-plane only); `ingestion_agent` demoted to is_admin=false (worker with explicit grants); EntityOverlay filters `ingestion_agent` out of the per-agent cards. Frontend bootstrap default key + DEFAULT_ACTING updated to `admin`. **Lineage carries per-write values:** `LineageRecord` gains an optional `value: Any | None` field; `_to_field_value` populates it for every candidate so 3 sources writing 250K / 300K / 285K render as three distinct chips, not three copies of the resolved value. **New endpoint:** `GET /documents/{id}/content` returns the markdown blob with source_class + label, scoped to the tenant (no ACL filter — source text is the input, not derived field value); SourceOverlay consumes it. **Source-class body renderers:** Gong-call / Jira-ticket / Gmail-email / Slack-thread / markdown-note variants in `panels.jsx` (`CallBody` / `TicketBody` / `EmailBody` / `SlackBody` / `NoteBody`), driven by `K.parseDocumentContent(doc)` in `helpers.js`. EntityOverlay header uses `[TYPE]` chip + plain key (`[CUSTOMER] Acme Corp`) instead of `entity · customer.acme_corp`. EntityOverlay's field rows are clickable → opens LineageDrawer to its left (drawer's `.shifted` class shifts right by 440px to clear the overlay). Test fix: `test_entity_visibility_denial_hides_all_fields` now reads as a non-admin (sales) since admin bypasses ACL. **Suite: 199/199 unit tests pass; ruff + format clean.**
- 2026-05-04 | step-10-pr32 | **PR 32 — d3-sankey reasoning graph + neighbor highlight + ingestion/graph tab split.** Replaced the hand-rolled bipartite SVG (per-edge animated dots, fixed-position columns, floating legend) with a d3-sankey layout served from unpkg (d3@7.9.0 + d3-sankey@0.12.3). Raw field-write edges (~100) aggregate into ~30 weighted ribbons per (source, target) pair; stroke width = field-count. Hover-driven neighbor highlight via mouseenter handlers that add `.hot`/`.dim` classes (CSS-driven 160ms transitions). Tiny entity labels on the long-tail low-traffic nodes are hidden by default and revealed on hover; `<title>` tooltip is the fallback. New `WorkPanel` wraps ExtractionPanel + GraphPanel as tabs and spans `grid-column: 1 / 3`, giving the active tab the full 2-column width. Default tab is Reasoning graph. End-to-end verified via chrome-devtools MCP. **197/197 unit tests pass; ruff + format clean.**
- 2026-05-04 | step-10-pr31 | **PR 31 — Demo cleanup: webui-demo-tenant + state-dir alignment + UI tab removal.** Renamed live tenant `local` → `webui-demo-tenant` in `tenants.json`. Deleted hardcoded `DEFAULT_LOCAL_TENANT` Python fallback so `tenants.json` is the only source of truth (missing file writes empty config + warning, no auto-populated default). `task reset` no longer deletes `tenants.json`. Aligned `.kentro_state/` everywhere (`kentro.toml`, `settings.py`, `migrations/env.py`) — Taskfile already used the dot-prefixed name; previously running the server via `uv run` vs `task` wrote to two different directories. Topbar: dropped the four prototype scene tabs (`01 · steady state` … `04 · lineage`) and the `local` tenant chip. SCENES list, scene state, goToScene controller all deleted. Test fixes: `store_test.py` asserts empty config (zero tenants) on missing file; `healthz_test.py` provides its own demo-key `tenants.json`. **197/197 unit tests pass; ruff + format clean.**

---

## 2026-05-03

- 2026-05-03 | step-10-pr15 | **PR 15 — Doc-type icons + de-duplicate Rego `package` header.** Two prototype-fidelity bugs the user caught after PR 14 landed. **(1) Every doc rendered as plain "doc" instead of the prototype's per-type icon + label** (📞 Call / ✉️ Email / 🎫 Ticket / 📝 Note). Root cause: `seed-demo` (CLI + `POST /demo/seed`) never set `DocumentRow.source_class`, so the UI's mapping had nothing to dispatch on. Fix: new `kentro_server.demo.infer_source_class(filename)` helper that maps corpus filenames to `verbal` / `email` / `ticket` / `note` (or `None` for unknowns); both seed paths now pass it. New `K.docMeta(doc)` JS helper in `helpers.js` turns `{source_class, label}` into `{icon, typeLabel, sourceClass}` with filename-pattern fallback for older state. ExtractionPanel's doc list + GraphPanel's SVG source nodes both use it. **(2) PolicyEditor right pane repeated `package kentro.access` 28 times** (once per rule). Fix: new `kentro.rules.render_rule_as_rego_body(rule)` SDK helper that returns the rule body without the `package ...` preamble + new `kentro.rules.rule_package_for(rule)` helper. `RenderedRule` server-side gains `rego_body: str` and `package: str` fields. UI's PolicyEditor groups `rendered.rules` by package and emits one dashed-border `<div class="policy-package-header">package kentro.access · 28 rules</div>` per group, then renders each rule's body only — no preamble repetition. New CSS section `.policy-package-group/.policy-package-header/.policy-package-tag/.policy-package-name/.policy-package-count`. **End-to-end Chrome verification:** doc list shows mixed icons (`📝 Note`, `🎫 Ticket`, `✉️ Email`, `📞 Call`); reasoning graph SVG shows the same per-type labels on source nodes; policies pane shows `package kentro.access · 29 rules` once at the top, then `[allow] ingestion_agent ...` lines per rule. **5 new unit tests in `demo_helpers_test.py`** cover every `infer_source_class` bucket. **197/197 unit tests pass; ruff + format + ty clean.**

- 2026-05-03 | ui-redo | **PR 14 — UI redo: live demo now matches `prototype.zip` layout exactly.** The previous 3-pane "agent switcher" UI was rejected ("not even close to the prototype"); replaced from scratch with the prototype's full 6-panel grid driven by real server data. **Layout (matches prototype.zip `app.jsx`):** topbar = brand · KENTRO · tenant chip (live from `/demo/keys`) · scene stepper (4 buttons) · rule-version chip · conflict-policy chip; main grid = 2 rows × 3 cols using the existing `.main` class — top row `[Sales agent | CS agent | Policy editor]`, bottom row `[Extraction | Reasoning graph | Access matrix]`; `<LineageDrawer>` slides in from the right when any field is clicked. **No agent-switcher dropdown** — both agent panels render simultaneously, each fetching `GET /entities/...` with ITS OWN bearer token via the new `K.api.readEntityAs(agentId, type, key)` helper, so Sales reading $300K and CS reading UNRESOLVED can be seen at the same instant. **Three new server endpoints** so the UI can be 100% real-data: (1) `GET /documents/{id}/extraction-steps` returning `ExtractionStepView[]` keyed via `FieldWriteRow.source_document_id` → distinct step ids → `ExtractionStepRow` rows, with `produced_writes` count per step; 404 on unknown UUID to avoid tenant-leak probes. (2) `GET /viz/access-matrix?entity_type=X` computes the per-(agent×field) read/write/visible matrix via `kentro.viz.access_matrix(...)` over the active ruleset; flat-cells shape for the JS table. (3) `GET /viz/graph` returns a bipartite `(document, entity)` graph keyed off `FieldWriteRow`s, deduped on `(doc, entity, field, agent)`. (4) Bonus: `GET /rules/active/rendered` pairs each Rule with `render_rule(...)` summary + `render_rule_as_rego(...)` snippet so the policy editor's expandable Rego pre-tags are server-rendered (no client-side rule-shape duplication). **SDK types added in `kentro/types.py`:** `ExtractionStepView`, `ExtractionStepListResponse`, `AccessMatrixCellView`, `AccessMatrixView`, `GraphNode`, `GraphEdge`, `GraphView` (six new wire types; SDK is still single source of truth). **JSX rewrite:** new `static/app.jsx` (`<App>`, `<AgentPanel>`, `<PolicyEditor>`, `<SceneStepper>` controller); new `static/panels.jsx` (`<ExtractionPanel>`, `<GraphPanel>`, `<AccessMatrixPanel>`, `<LineageDrawer>`). Topbar's tenant chip + rule chip + conflict chip all live-derive from server state on every refresh. Per-field click in either AgentPanel opens the LineageDrawer for that (entity, field), reading via that agent's bearer. The four scene buttons fire real server ops sequentially: Scene 1 = `POST /demo/seed` if empty, Scene 2 = ingest `email_jane_2026-04-17.md` if missing, Scene 3 = NL-parse + apply "written outweighs verbal" via `/rules/parse` → `/rules/apply`, Scene 4 = open lineage drawer on `Customer/Acme Corp/deal_size`. **Deleted:** `static/agent-switcher.jsx` (concept retired). **Kept:** `static/escalation-toast.jsx` unchanged (still useful for SkillResolver NotifyAction events); `static/styles.css` reused unchanged (the prototype's complete 1700-line design system was already shipped in PR 12). **Tests:** 5 new in `api_smoke_test.py` covering `/documents/{id}/extraction-steps` (happy path + 404), `/viz/access-matrix`, `/viz/graph`, `/rules/active/rendered`. **Suite: 192 unit tests pass (was 187); ruff + format + ty clean.**

- 2026-05-03 | step-13-pr | **PR 13 — Codex 2026-05-03 review fixes (4 findings bundled).** Closes the four outstanding adversarial-review findings on top of PR 12. **(1) [HIGH #1] SkillResolver actions deduped on the read path.** New `SkillActionExecutionRow(scope_key, action_fingerprint, ...)` table + `UNIQUE(scope_key, action_fingerprint)` constraint blocks re-execution across reads. `core/read.py::_scope_key_for_decision` keys off the open `ConflictRow.id` when ≥2 distinct candidate values exist, else off the winning `FieldWriteRow.id` — both stable across retries. `_action_fingerprint` is a SHA-256 over the action's normalized payload (sorted JSON). `_execute_actions` checks first, then inserts after success; an `IntegrityError` race is logged and silently de-duped. Alembic revision `bac115dd4042_add_skill_action_execution_dedupe` adds the table; down_revision points at `9e3efe52ae84`. Existing `test_skill_action_notify_publishes_to_event_bus` extended to call the read TWICE and assert exactly one event landed on the bus. **(2) [HIGH #2] MCP `kentro_remember` now atomic via `write_fields_bulk`.** Mirror of the HTTP `routes/memory.py::remember` fix from PR 10-1; the per-field loop is gone. New `tests/unit/mcp_smoke_test.py::test_kentro_remember_atomic_no_partial_writes_on_field_denial` runs the same assertion as the HTTP-side `test_remember_atomic_no_partial_writes_on_field_denial` against the MCP tool. **(3) [HIGH #3] `SchemaRegistry.register_many` is now atomic.** Pre-validates every (existing | None, new) pair as a pure check before any DB write; if any pair fails the evolution rules, raises `SchemaEvolutionError` with the offending type name and zero rows are persisted. The apply phase runs inside one `session.begin()` block so a DB-level error rolls back the whole batch. Two new tests: `test_register_many_is_atomic_on_evolution_failure` (third type fails → no partial commit) and `test_register_many_writes_all_when_every_type_validates`. **(4) [MEDIUM #4] EventBus is thread-safe.** `__init__` captures the running loop via `asyncio.get_running_loop()` (fails loudly if constructed outside a loop — production lifespan and tests both already are). `publish()` routes per-subscriber `put_nowait` through `loop.call_soon_threadsafe`, so cross-thread publishes from sync FastAPI handlers no longer corrupt asyncio-queue state. New `tests/unit/events_test.py` covers same-loop publish, worker-thread publish (via `asyncio.to_thread`), 16-event burst from a thread (no loss), and the construct-outside-loop failure mode. **Verification:** `task gates` green (188 unit tests + ruff + format + ty); `task reset-and-seed` produces a fully populated tenant (4 schemas + 29 rules + 8 docs ingested → 36 entities); migration upgrades cleanly from initial baseline to head.

- 2026-05-03 | design | **PR 12 — Design refactor: live UI now uses the prototype's design tokens.** All inline-style React components rewritten as class-based components consuming the existing `styles.css` tokens (oklch palette: `--accent` electric indigo, `--pop` magenta, `--warn` amber, `--ok` green, `--ink` ink scale; `--mono` JetBrains Mono + `--sans` Inter; backdrop-blur glass panels; soft shadow scale). **(1) `styles.css` extended** with ~430 lines of new component classes that use the same tokens: `.live-shell` + `.live-main` (3-column layout), `.agent-switcher` (header bar with brand-mark + colored bootstrap-state dot), `.seed-overlay` (empty-tenant hero with prominent indigo seed button), `.admin-badge` (the `↑admin` chip in `--accent-soft` background), `.doc-row` + `.ingest-form`, `.entities-section` + `.entity-row`, `.policy-editor` + `.parsed-preview` + `.rule-line.added` (green diff-highlight), `.entity-drawer` (slide-over with `.field-card` + `.lineage`), `.toast-stack` + `.toast` (escalation-toast). Every component reuses the prototype's `.field-status.status-known/-hidden/-unresolved/-unknown` pills via the new `<StatusPill>` helper. **(2) `app.jsx` rewritten** — every `style={{...}}` block replaced with `className=`. New `<PanelHead>` helper renders the prototype's `.panel-head` + `.panel-title` (with the indigo→magenta stripe-mark `::before`) + optional sub/right slots. New `<AdminBadge>` for elevated controls. **(3) `agent-switcher.jsx` & `escalation-toast.jsx`** rewritten the same way. **Visual verification via Chrome MCP:** populated 3-pane view (admin) shows glass panels with indigo accents on doc rows, monospace meta lines, properly-styled ingest button + delete buttons with `↑admin` badge in admin-soft background, two-pane policy editor with NL textarea + apply button stacked under heading; entity drawer slides in from the right with green KNOWN pills + monospace lineage rows + glass background; empty-tenant overlay shows the seed-data hero with accent-colored entity-type chips + prominent indigo button + monospace `task reset-and-seed` callout. The live UI is now visually consistent with the original prototype's design language. **180/180 unit tests pass; ruff + format + ty clean.**

- 2026-05-03 | step-11-pr | **PR 11 — SkillResolver prompt updated to emit workflow actions + end-to-end test.** The plumbing from PR 10-5 only produces toasts when the LLM actually emits `actions`; this PR teaches it. **(1) `skills/skill_resolver/SKILL.md` rewritten.** Now describes the two-job protocol: (a) pick a winner, (b) optionally emit `WriteEntityAction` + `NotifyAction` items when the policy implies a side effect. Two few-shot examples in the prompt: one with side effects (written-vs-verbal + log to AuditLog + notify #deals-review), one without (latest-write only). The cache-key fingerprint includes the rendered system prompt, so this edit invalidates every cached SkillResolver response by construction (the production cache + the offline `OfflineLLMClient` test path both flow through). **(2) `tests/unit/api_smoke_test.py::test_skill_action_notify_publishes_to_event_bus`** — end-to-end test: register Customer + apply ConflictRule(SkillResolver) + write two conflicting deal_size values + script `FakeLLM.skill_decision` to return a winner + `NotifyAction(channel="#deals-review", ...)` + monkey-patch `app.state.event_bus.publish` to capture + GET /entities/Customer/Acme + assert exactly one `notify` event landed on the bus with the right tenant_id and channel. Proves the full wire: SkillResolver decision → ResolvedFieldValue.actions → core/read.py::_execute_actions → EventBus.publish → SSE-ready event. **(3) Synthetic corpus audit.** No regeneration needed — the 8 markdown files in `examples/synthetic_corpus/` (~190 LOC total) already exercise the four entity types (Customer, Person, Deal, AuditLog), the verbal-vs-written distinction, and the conflicting deal_size values that drive Scene 2-4. The end-to-end smoke test (`tests/integration/end_to_end_smoke_test.py`, gated on `ANTHROPIC_API_KEY`) walks the full pipeline against this corpus today. **180/180 unit tests pass; ruff + format + ty clean.** Memory note `pending_workflow_aware_skills.md` was retired in PR 10-5; PR 11 closes the LLM-emission half of the deferral.

- 2026-05-03 | step-10-pr5 | **PR 10-5 — Workflow-aware Skills (server-side plumbing + UI toast).** The server-side surface for the "memory is the workflow trigger" demo beat lands; UI subscribes via SSE and renders fade-in toasts. Skill prompt update (so the LLM actually emits actions) is deferred to PR 11. **(1) `SkillResolverDecision.actions`** — discriminated union: `WriteEntityAction(entity_type, entity_key, field_name, value_json)` and `NotifyAction(channel, message)`. The skill prompt template can now ask the LLM to emit these alongside the winner pick. **(2) `ResolvedFieldValue.actions`** — `core/resolve.py` propagates decision actions onto the result so `read_entity` can dispatch them. New unit test `test_skill_resolver_passes_decision_actions_through_resolved_field_value` proves the round-trip. **(3) `core/read.py::_execute_actions`** — walks `resolved.actions` after each field's resolve. `WriteEntityAction` goes through `write_field()` which re-evaluates ACL for the acting agent (Skills cannot bypass governance). `NotifyAction` publishes onto the EventBus. Best-effort: one failed action does not abort the others; each logs. **(4) `core/events.py::EventBus`** — in-process fan-out asyncio-queue broadcaster, attached to `app.state.event_bus` in the lifespan. Per-subscriber queue (maxsize=64); slow subscribers drop events with a warning. Tenant-scoped: every `Event` carries `tenant_id`. **(5) `GET /events`** — Server-Sent Events endpoint. Bearer-authed; subscribes its own queue; filters events by tenant_id; heartbeat every 30s (SSE `:` comment) to keep proxies from idling out. SSE chosen over websocket: simpler, same HTTP path as everything else, no upgrade handshake. **(6) `api/deps.py::EventBusDep`** + entities route plumbing — `read_entity` accepts an optional `event_bus` so the in-process pure-function path stays optional-side-effect. **(7) `static/escalation-toast.jsx`** — `<EscalationToast>` opens `/events` via `fetch()` (EventSource doesn't support Auth headers) with the active agent's bearer; manually parses SSE blocks (event: + data: lines split by `\n\n`); pushes `notify` events into a fade-in toast list with auto-dismiss after 6s. Reconnects on agent switch. Mounted in App. **Note on Skill prompt:** The LLM doesn't yet emit `actions` because the SkillResolver prompt template still doesn't ask for them. PR 11 (or a follow-up) updates `skills/skill_resolver/SKILL.md` to include action-emission instructions + few-shot examples. The plumbing tested here ensures that when the prompt is updated, actions flow end-to-end. **179/179 unit tests pass; ruff + format + ty clean.**

- 2026-05-03 | step-10-pr4 | **PR 10-4 — Stage C: data.js deleted; in-UI seed button.** The static UI bundle has zero canned data; everything the user sees comes from the live server. **(1) `POST /demo/seed`** — new admin-only + opt-in-gated server route that does what `kentro-server seed-demo` does, in-process: registers the 4 demo schemas, applies the 29-rule canonical ruleset, ingests every markdown in `examples/synthetic_corpus/`. Path: `parents[6]` from `routes/demo.py` walks back to repo root (one off-by-one fix caught via Chrome). Returns `{tenant_id, schemas_registered, rule_version, rules_applied, documents_ingested}`. **(2) `K.api.seedDemo()`** — `POST /demo/seed` admin-elevated. **(3) Empty-tenant overlay.** App detects `documents.length === 0 && schemaCount <= 1` (Note auto-seeds → 1 isn't really empty) and renders a centered "EMPTY TENANT · seed demo data ↑admin" hero in lieu of the 3-pane layout. On click: spinner → `seedDemo()` → `bumpRefresh()` → next render hits the populated layout. **(4) `data.js` deleted** plus `panels.jsx` and `tweaks-panel.jsx` (dead since PR 10-2; `panels.jsx::LineageDrawer` was the last KENTRO_DATA reference). `index.html` no longer loads any of those files. `git grep KENTRO_DATA packages/` returns zero hits. **End-to-end Chrome verification:** with a freshly reset state dir, page renders the empty-tenant overlay; clicking "seed demo data" hits `POST /demo/seed` (~30s real LLM extraction); on completion the page re-renders into the populated 3-pane view with 29 rules, all 4 entity types populated, and 8 documents listed. The UI now contains zero canned data — every byte the user sees came from `kentro-server`. **178/178 unit tests pass; ruff + format + ty clean.**

- 2026-05-03 | step-10-pr3 | **PR 10-3 — Stage B: writes + two-pane policy editor.** UI write path + the policy-editor surface land. **(1) `window.K.api` write surface.** New methods: `applyRules(ruleset, summary)` with `elevateToAdmin: true`, `parseNL(text)` (active-agent token), `writeField(type, key, field, value_json, confidence)`, `ingestDocument(content, label, source_class)` (active agent — needs write grants), `deleteDocument(document_id)` admin-elevated. Auto-elevation pattern: `_fetch(path, {elevateToAdmin: true})` swaps the bearer for the cached admin key regardless of the active-agent dropdown. **(2) `IngestForm` (in DocumentsPanel header).** Expandable form: label input + `written/verbal/system` source-class select + content textarea + `ingest`/`cancel`. POSTs through `K.api.ingestDocument`; on success bumps the App's refresh counter so documents + entities re-fetch. **(3) `× del ↑admin` button per document row.** Confirms via `window.confirm` (cascade warning), DELETEs through admin-elevation, refreshes on success. The green `↑admin` badge sits on every elevated control so the demoer can see "this still works as Sales but it's actually using the admin token." **(4) `PolicyEditor` replaces the read-only `RulesPanel` on the right pane.** Top: NL chat textarea + `parse`/`apply ↑admin` buttons. After `parse`: shows "PARSED · N new rules · M intents", lists rules in green `+` form, shows the `notes` string. After `apply`: posts the additive ruleset (existing rules + new), bumps version, triggers a 4-second green `+` highlight on newly-added rows in the structured view below. Bottom: active ruleset grouped by rule type, rendered with the same `[allow] sales reads Customer.name` shape from `kentro.rules.render_rule`. **(5) Diff helpers.** `ruleKey(r)` produces order-independent canonical-JSON identity for a rule; `diffRulesets(old, new)` returns `{added, removed}`. Mirrors `kentro.rules.ruleset_diff` semantics client-side so the highlight doesn't need a server round-trip. **(6) NLResponse shape fix.** First parse round-trip crashed with `(parsed.notes || []).map is not a function` — `notes` is `str | None`, not list. Switched to plain conditional rendering. **(7) App refactor.** `refresh` counter shared via `bumpRefresh` callback so EntitiesPanel + DocumentsPanel re-fetch after any write (ingest, delete, apply). EntitiesPanel's local `refresh()` renamed to `reload()` to avoid the prop shadowing. **End-to-end Chrome verification:** all UI pieces render correctly under all 3 agents; ingest form opens with proper layout; `× del ↑admin` button visible per row; NL chat → POST /rules/parse → 200 with `PARSED · 0 NEW RULES · 1 INTENTS` + `note: intent '...': rule JSON did not match Rule schema` (LLM round-trip works structurally; rule-compilation rate is a server-side prompt-quality concern out of scope for Stage B); `apply` button correctly disabled until parser produces a compilable rule. **178/178 unit tests pass; ruff + format + ty clean.** PR 10-4 (delete data.js + in-UI seed button) opens next.

- 2026-05-03 | step-10-pr2 | **PR 10-2 — Stage A: live UI driven by real server reads.** The prototype's static HTML/JSX page now renders entirely from the kentro-server HTTP API; `data.js` stays only as a deletable fallback. **(1) `static/api.js`** — `window.K.api` thin client: `bootstrap()` (calls `GET /demo/keys` once, caches per-agent bearer tokens in localStorage), `setActingAs(agent_id)` / `getActingAs()` (acting-as state with `kentro:actingAsChanged` event), `listDocuments`, `listEntities(type)`, `readEntity(type, key)`, `getRules` (→ `/rules/active`), `listSchema`, `getStats`. `_fetch(path, {elevateToAdmin})` injects the active-agent token by default; admin-only routes will set `elevateToAdmin=true` in PR 10-3 with a visible `↑ admin` badge. **(2) `static/agent-switcher.jsx`** — header dropdown showing all 3 agents with a green/red bootstrap-status dot. Bootstrap fires `kentro:bootstrapped` once keys are cached so consumer panels know it's safe to fetch. **(3) `static/app.jsx` rewritten** — three-column grid: Documents (left, from `GET /documents`), Entities-by-type (center, fetched per `acting` agent + filtered server-side by `EntityVisibilityRule`), Active Ruleset (right, from `GET /rules/active` grouped by rule type, rendered with the same `[allow] sales reads Customer.name` shape as `kentro.rules.render_rule`). Click an entity row → `EntityDrawer` opens (right edge) with the full `GET /entities/{type}/{key}` response — every field rendered with a status pill (`KNOWN` / `UNKNOWN` / `HIDDEN` / `UNRESOLVED`) + lineage list. Drawer refetches on agent switch. (4) **Race-condition fix** — App and panels gate their fetches on a `ready` flag that flips true after `kentro:bootstrapped` fires; without this, panels paint with "no bearer token available" errors before bootstrap completes. (5) **Bug fix in resolve dispatcher** — a `ConflictRule(resolver=AutoResolverSpec())` previously dispatched AutoResolver → AutoResolver and fell through to `TypeError`. Now `_auto_dispatch` detects the self-reference and uses the `LatestWriteResolver` fallback with a warning log. New unit test covers the path. (6) **Demo-ruleset cleanup** — removed the meaningless `ConflictRule(field_name="contact", resolver=AutoResolverSpec())` from `initial_demo_ruleset()` (it was tripping the bug above). 30 → 29 rules; `Customer.contact` falls back to AutoResolver via the read-path default, which is the same effective behavior. (7) **Taskfile fix** — `reset-and-seed` now runs the background uvicorn invocation through `bash -c '...'` so `$!` captures the real PID instead of a zsh literal. (8) **Cleanup** — `index.html` no longer loads `panels.jsx`/`tweaks-panel.jsx` (those depended on `KENTRO_DATA` and are dead in the new view; full deletion is PR 10-4). **Verified end-to-end via Chrome MCP:** `task reset-and-seed` → admin view shows all 4 entity types + 36 entities + 8 documents + 29 rules; switching to Sales hides AuditLog (visibility deny) and Person/Note (no grant); switching to CS reveals AuditLog (1 entity) and hides Deal/Person/Note; opening Acme Corp drawer as CS shows `name`/`contact`/`support_tickets` with `KNOWN` pills + values, but `deal_size` and `sales_notes` show purple `HIDDEN` pills with the ACL reason (`no rule grants access`) — proving real `evaluate_field_read()` runs through the live HTTP path. **178/178 unit tests pass; ruff + format + ty clean.** PR 10-3 (writes + policy editor) opens next.

- 2026-05-03 | step-10-pr1 | **PR 10-1 — Foundation: codex 3-finding fix + Alembic + seed-with-ACLs + read endpoints.** First of six staged PRs that take Step 10 from "static UI prototype mounted" to "real working server, no canned data". Every line of `data.js` becomes deletable in PR 10-4 because of the plumbing this PR puts down. Highlights: **(1) Codex critical fix — auth-default inverted.** `kentro_prod_mode` retired; replaced with `kentro_allow_demo_keys: bool = False`. The lifespan now refuses to boot when committed `local-*-do-not-share` keys are present in `tenants.json` UNLESS `KENTRO_ALLOW_DEMO_KEYS=true` is set explicitly. Failure mode is now "operator must opt INTO using demo keys" instead of "operator must remember to flip on prod-mode". `task dev` and `task reset-and-seed` set the env var so local dev still "just works"; deployments must rotate keys or set the flag explicitly. **(2) Codex high fix — atomic `/memory/remember`.** New `core/write.py::write_fields_bulk(...)` evaluates ACL for every field UP-FRONT against one loaded ruleset; if any field would deny, none are written. Inside the DB transaction, `session.begin()` ensures a DB-level exception on field N rolls back fields 1..N-1. The route now writes all 4 fields (subject/predicate/object_json/source_label) atomically. New test `test_remember_atomic_no_partial_writes_on_field_denial` proves it. **(3) Codex high fix — Alembic with single shared `versions/`.** New `migrations/env.py` knows about `SQLModel.metadata` for autogenerate AND iterates `<state_dir>/<tenant>/state.sqlite` for upgrade. New `store/migrations.py::upgrade_to_head(path)` called from `TenantStore.__init__` replaces `metadata.create_all()` — every fresh tenant DB lands at current head and stamps the `alembic_version` table. Initial revision `9e3efe52ae84_initial_baseline.py` autogen'd from current models (9 tables + indices). `script.py.mako` template uses modern `X | Y` typing so generated revisions match the rest of the codebase. **(4) Demo-rules baked into seed-demo.** `kentro_server.demo.ruleset.initial_demo_ruleset()` returns the canonical 30-rule Scene-1 starting world (Sales reads all Customer fields; CS reads only contact-shaped fields; AuditLog hidden from Sales; latest-write conflict resolver on `Customer.deal_size`; ingestion_agent permissive grants). `seed-demo` CLI now POSTs this ruleset between `/schema/register` and `/documents` ingestion (with `--skip-rules` opt-out for tests). Right pane in the UI is non-empty on a fresh tenant; ingestion writes are no longer silently dropped by default-deny. **(5) Read endpoints for Stage A.** `GET /entities/{type}` returns `EntityListResponse` (tuple of `EntitySummary` filtered by `EntityVisibilityRule`); `GET /documents` returns `DocumentListResponse` (sorted by `created_at desc`, `field_write_count` per row). Both ACL-aware, both used by the prototype's left-pane lists in PR 10-2. New SDK types: `EntitySummary`, `EntityListResponse`, `DocumentSummary`, `DocumentListResponse`. **(6) `GET /demo/keys` admin endpoint.** Returns the per-agent bearer tokens for the current tenant; admin-gated AND opt-in-gated (404 unless `KENTRO_ALLOW_DEMO_KEYS=true`, even for admin). Lets the demo UI's agent-switcher pre-populate localStorage on first load instead of forcing copy-paste from `tenants.json`. New `kentro_server.api.routes.demo` router + `demo_router` exported. **(7) Taskfile additions:** `task migrate` (alembic upgrade against every tenant DB; idempotent), `task migrate:revision -- "message"` (autogen against the first tenant DB), `task reset-and-seed` (wipe → background uvicorn → wait for /healthz → migrate → seed schemas+ACLs+corpus → leave server up), `task reset-and-seed:stop` (kill the backgrounded server). One-command path from cold to fully-loaded demo. New tests: 6 (atomic remember, demo-key boot guard, /entities list ACL filtering, /documents list, /demo/keys admin gate, /demo/keys 404 without opt-in). **177/177 unit tests pass; ruff + format + ty clean.** End-to-end smoke verified via `task reset-and-seed`: 4 schemas registered + 30-rule ACL applied + 8 corpus docs ingested → 36 entities; `/entities/AuditLog` returns `[]` to sales (hidden) but populated to admin; `/demo/keys` returns 200 to admin + 403 to sales. Memory note `pending_codex_2026_05_03_fix_pr.md` retired (codex bundle is closed by this PR). Existing pre-PR-#18 tenant DBs need delete-and-rebuild (this is the first Alembic-stamped baseline; documented in IMPLEMENTATION_PLAN.md Step 10 v2 plan).
- 2026-05-03 | review | **Codex adversarial review on whole codebase + style-sweep follow-up.** Codex returned `needs-attention` with 3 findings: (1) **[critical]** `kentro_prod_mode=False` default boots happily with the committed `local-*-do-not-share` keys on a non-loopback bind — recommend inverting the failure mode (refuse boot when demo keys present unless `KENTRO_ALLOW_DEMO_KEYS=true` set); (2) **[high]** `/memory/remember` calls `write_field()` per-Note-field in a loop with no transaction, so a deny mid-loop persists a half-written Note; (3) **[high]** `metadata.create_all()` doesn't `ALTER TABLE` — `DocumentRow.source_class` (added in PR #18) needs manual `ALTER TABLE` against existing tenant DBs. **User decision: bundle all three into a single deferred fix-PR with Alembic + single shared `alembic/versions/` for #3.** Full PR scope captured in `IMPLEMENTATION_PLAN.md` "Deferred to the very end" + memory note `pending_codex_2026_05_03_fix_pr.md` (surface BEFORE any persisted-table change OR Step 12, since Alembic is part of the bundle). Also addressed in this commit (cheap to ship now): (a) CLAUDE.md gains a "**Codex review default — `--wait`**" rule (always foreground unless user types `--background`); (b) CLAUDE.md gains a "**No quoted forward-reference annotations**" rule under Code Style — caught by ruff F821 only because the original `_mcp_redirect` had `-> "RedirectResponse":` paired with a function-local import; the rule's narrow legitimate use is genuine same-module forward refs, everything else hoists the import. (c) Sweep: `tenant_store.TenantRegistry.from_paths` and `tenant_config.TenantConfig._agents_unique` / `TenantsConfig._no_duplicate_ids_or_keys` switched from quoted `-> "Foo"` to `typing.Self`; mid-function `from uuid import uuid4` and `from kentro_server.core.schema_registry import SchemaEvolutionError` hoisted in `sdk_client_test.py`, `schema_test.py`, `api_smoke_test.py`. **171/171 tests pass; ruff + format + ty clean.**
- 2026-05-03 | step-10 | **Step 10 bootstrap — static UI prototype mounted, Taskfile, demo-schema rounding.** Branch `serve-static-ui-prototype`. (1) Copied the Claude-Designed React-via-CDN prototype (`app.jsx`, `panels.jsx`, `tweaks-panel.jsx`, `data.js`, `helpers.js`, `styles.css`, icon, `uploads/`) into `packages/kentro_server/src/kentro_server/static/`; renamed "Kentro Demo.html" → `index.html` so `StaticFiles(html=True)` serves it at `/`. Removed an empty `kentro/project/` design-tool junk dir. No build step (CDN React + Babel-in-browser). (2) Mount order fix in `main.py`: `app.mount("/", StaticFiles(...))` lives **after** all explicit `@app.get` decorators (`/healthz`, `/llm/stats`) and after the `/mcp` Starlette mount, so static doesn't shadow them. Added an explicit `@app.get("/mcp")` 307 → `/mcp/` redirect because Starlette's `Mount("/mcp")` only matches paths with the trailing slash; without the redirect, hitting `/mcp` would 404 against the catch-all StaticFiles. Three new regression tests in `healthz_test.py`: `test_static_ui_served_at_root` (asserts the prototype `<title>` lands on `/`), `test_static_ui_serves_named_files` (asserts `/app.jsx` returns JS), `test_mcp_mount_still_works_after_static` (asserts `/mcp` returns 200 or 307, never 404). (3) Demo schema set rounded out: `Deal(customer, size, stage)` and `AuditLog(events)` Entity subclasses added to `kentro_server.demo.schemas`; `seed-demo` now registers all four (`Customer`, `Person`, `Deal`, `AuditLog`) so the prototype's 4-entity seed has real backing types. (4) `DocumentRow.source_class: str | None = Field(default=None, index=True)` added so the prototype's source-class pills (transcript / email / meeting / note) can be sourced from real data; plumbed through `IngestRequest` → `routes/documents.py` → `ingest_document(..., source_class=...)`. Existing DBs need delete-`kentro_state`-to-rebuild or manual `ALTER TABLE`. (5) `kentro.rules.render_rule_as_rego(rule) -> str` (display-only Rego rendering — never parsed) for the policy-editor's enterprise-aesthetic right pane. Per-variant shapes: allow/deny on `FieldReadRule`, wildcard handling on `WriteRule`, `entity_key` inclusion on `EntityVisibilityRule`, canonical written-vs-verbal Rego for `SkillResolverSpec` whose prompt mentions both "written" and "verbal" else generic `skill_pick` fallback, `LatestWriteResolverSpec` → `exists_newer`, `PreferAgentResolverSpec` → name in winner block, `RawResolverSpec` → `unresolved[field]`. 10 new tests in `rules_helpers_test.py`. (6) `Taskfile.yaml` at repo root (`brew install go-task`): `dev`, `open`, `seed`, `reset`, `test`, `test:integration`, `lint`, `format`, `format:check`, `typecheck`, `gates`, `smoke`. Replaces the originally-proposed `deploy.sh`. **All unit tests + format + lint + ty clean.** Still pending for Step 10 done: wire prototype `data.js` to real HTTP API, build the two-pane policy editor against `viz.access_matrix()`/`viz.rule_diff()`, ship `<TicketBadge>`/`<EscalationToast>` once `SkillResolverDecision.actions` lands.
- 2026-05-03 | docs | **Reframed HumanReviewResolver as a workflow-aware Skill across docs.** The original 2026-05-01 design used a `HumanReviewResolver(inner_resolver=..., trigger=...)` Python wrapper class that fired Ticket creation + Slack notification on threshold-matched conflicts. After a 2026-05-03 design conversation, that becomes one shape a `SkillResolver` can take: the Skill's markdown file carries BOTH the policy ("how to pick a winner") AND the workflow ("what else to do alongside the pick — create a Ticket, fire a notification"). No new resolver class. Admin authors workflow in markdown, not Python — aligns with the existing skill-loader pattern. Server-side surface is `SkillResolverDecision.actions: tuple[SkillAction, ...]`; the orchestrator in `core/resolve.py` walks each action through the same `write_field` ACL gate. **Code change deferred** — TODOs added at both sites (`skills/llm_client.py`, `core/resolve.py`); IMPLEMENTATION_PLAN.md "Deferred to the very end" carries the full implementation plan; memory note `pending_workflow_aware_skills.md` set to surface BEFORE Step 10 begins (UI components depend on this surface). Docs updated in BOTH `~/Claude-Cowork/fundraising/` and the repo: `demo.md` Scene 4 + lineage panel + scope sections; `implementation-handoff.md` Read path + Step 5 + Step 10; `memory.md` voice anchors + SDK locked decisions + decision history. Demo's "Memory is the workflow trigger" framing stays — now grounded in skill-driven workflow rather than wrapper-class side effects.
- 2026-05-03 | step-9 | **Step 9 done — viz helpers + CLI renderers + acl.py moved to SDK.** New `kentro/viz.py` with four pure data transformations: `access_matrix(ruleset, agents, entity_type_defs) -> AccessMatrix` (the right-pane default view's data shape — rows × cols × cells with read/write/visibility per cell), `rule_diff(old, new) -> RuleDiffView` (wraps `ruleset_diff` and groups by rule type with total_added/total_removed for the panel header), `lineage(record) -> LineageView` (per-field flattening of EntityRecord lineage; one entry per LineageRecord for KNOWN, one per candidate for UNRESOLVED), `conflicts_from_records(records) -> ConflictsView` (surfaces UNRESOLVED fields; server-side `GET /conflicts` deferred). New `kentro_server/viz_cli.py` with Rich-based CLI renderers — `print_access_matrix` (Rich Table with ✓/✗/●/○ glyphs, deliberately terse so wide schemas fit), `print_rule_diff` (Rich Panel per non-empty section, green `+` / red `−`, with `escape()` so render_rule's `[allow]`/`[skill]` literal brackets aren't eaten by Rich's markup parser), `print_lineage`, `print_conflicts`. Refactor: `kentro_server/core/acl.py` moved to `kentro/acl.py` (ACL eval is pure over `RuleSet`/`Rule` SDK types; belongs in the SDK so notebooks can use it without the server). Six call sites bulk-updated. Re-exported `evaluate_field_read` / `evaluate_entity_visibility` / `evaluate_write` / `AclDecision` from `kentro.__init__`. New tests: 10 in `viz_test.py`. **158/158 unit tests pass; ruff + format + ty clean.** Deferred: `entity_graph` (data model has no entity-to-entity foreign keys; meaningful "graph" is already exposed via lineage), server routes for historical RuleSet versions, `GET /conflicts` route — surfaced as v0.1 follow-ups in IMPLEMENTATION_PLAN.md Step 9.
- 2026-05-03 | step-8 | **Step 8 done — SDK `kentro.Client` + rules helpers + Note.subject fix.** Single sync `Client` class wraps every HTTP route 1:1 (`packages/kentro/src/kentro/client.py`). Status-code dispatch maps to typed exceptions (`AuthError` 401, `AdminRequiredError` 403, `NotFoundError` 404, `SchemaEvolutionError` 409, `ServerError` 5xx, `KentroError` base) — admin-ness is the server's call, the SDK does no role-checking. Constructor takes optional `transport=httpx.BaseTransport` for in-process testing (used by tests via `httpx.MockTransport` rerouting through `TestClient(app)`) — production callers leave it `None`. Rules helpers (`packages/kentro/src/kentro/rules.py`): `RuleSetDiff` dataclass + `ruleset_diff(old, new)` (set-difference of rules by canonical JSON, order-invariant) + `render_rule(rule) -> str` (one-line text per Rule variant, including all five ResolverSpec types). Both live in the SDK package per Decision 2.7 so notebooks/UI/CLI consumers can use them without a server roundtrip. **Note.subject populate-on-write** closed (the v0.1 cleanup logged on 2026-05-03 from the lineage walkthrough): `routes/memory.py` and `mcp_server.py::kentro_remember` now write `subject` into the field so reads return the actual subject instead of UNKNOWN. New tests: 17 in `rules_helpers_test.py`, 15 in `sdk_client_test.py`, plus updated assertion in `api_smoke_test.py`. **148/148 unit tests pass; ruff + format + ty clean.** Two related items deferred to "Deferred to the very end" with memory reminders saved: (a) background dev-server convenience (`kentro.dev.LocalServer()` / `kentro-server demo` for the one-line notebook UX) — surface AFTER Step 10; (b) Witchcraft wiring into ingestion — surface BEFORE Step 11 (need to decide if demo depends on semantic search).
- 2026-05-03 | design | **ACL governance model + admin-editor surface locked.** Captured in IMPLEMENTATION_PLAN.md "Decisions locked since the handoff" §2 (with cross-references in Steps 8/9/10). Outcomes: keep the four existing rule types (`FieldReadRule`, `EntityVisibilityRule`, `WriteRule`, `ConflictRule`) — no new types for v0; conflict rules stay separate from access rules; `is_admin: bool` is omnipotent for v0 (granular permissions are v0.1); explicitly rejected adopting a PBAC language (Cedar / OPA Rego / Casbin) — the typed `RuleSet` IS the language, and adding a DSL middle step would worsen the demo narrative ("type English → DSL → typed rule" vs the current "type English → typed rule"). Admin editor is a two-pane UI (left = NL chat via `parse_nl_to_ruleset`, right = structured policy view), default right-pane rendering is an access matrix (rows=agents, cols=fields) with a sectioned-by-rule-type panel below for audit detail; on apply, changed rows highlight (added `+` green, removed `−` red) using a forthcoming `ruleset_diff()` helper. The structured view sells "enterprise" without a DSL by making the model itself visible (4 rule categories, agents × fields matrix, conflict policies as their own domain) — PBAC syntax can't do that. Server-side helpers needed for Step 10 to be thin: `core/rules.py::ruleset_diff(old, new)` and `core/rules.py::render_rule(r)` — both land in Step 8 alongside the SDK. v0.1 backlog explicitly enumerated (wildcards on `agent_id`, group/role resolution, predicate-based rules, granular admin permissions, `Note.subject` cleanup) so future conversations don't restart from zero. No code changes.
- 2026-05-03 | docs | **Tier 4 status corrected to ⚠️ partial + lineage-demo findings logged.** Followed the MCP path-doubling fix (commit `f14385b`) with manual MCP exploration from Claude Code as the client. Verified the happy paths for `kentro_list_schema`, `kentro_get_rules`, `kentro_apply_rules` (admin gate works through MCP), `kentro_remember` (object_json roundtrips non-string values correctly through MCP — confirms the High-#5 fix), and `kentro_read`. NOT yet covered: negative auth path, admin-gate denial through MCP, automated MCP-transport integration test. IMPLEMENTATION_PLAN.md updated to reflect Tier 4 as partial (was incorrectly being marked ✅). Logged two v0.1 follow-ups: `Note.subject` is a placeholder field that always reads UNKNOWN (kentro_remember writes subject as entity_key, never into the field) — either populate or drop; and add a real-transport MCP integration test so path-doubling bugs can't recur silently.
- 2026-05-03 | mcp-fix | **MCP `/mcp` path doubled — `/mcp/mcp` was the only working URL.** Caught by Tier 4 (real client connection): `claude mcp list` showed kentro as `Failed to connect`. Root cause: `FastMCP.streamable_http_path` defaults to `/mcp`, then we mount at `/mcp` on the parent FastAPI app — full URL becomes `/mcp/mcp`. Clients hitting `/mcp` got HTTP 307 → `/mcp/` → HTTP 404. Fixed by `FastMCP("kentro", streamable_http_path="/")` so the inner app routes at `/`; mounted at `/mcp` on the parent gives the canonical `http://host/mcp/` URL (HTTP 307 → `/mcp/` → 200, follow-redirect-friendly clients can use `/mcp`). 116/116 unit tests still green; ruff + format + ty clean.
- 2026-05-03 | post-merge | **Step 7 post-merge validation (Tiers 1, 2, 3, 3.5).** All four green. Caught two real bugs the unit suite missed: (a) `smoke-test` CLI command failed against default-deny ACL because it never set up its own grants — fixed by having the command apply a minimal permissive ruleset using the admin key it's given; (b) committed `tenants.json` predated the `is_admin` field, so the merged main shipped with no admin agent — fixed by adding `is_admin: true` to `local:ingestion_agent`. Tier 1: server boots, /healthz works, 7 curl admin-gate checks pass (admin /rules/apply 200, non-admin 403; non-admin /schema/register 403; non-admin DELETE /documents 403; missing/unknown bearer 401), smoke-test CLI passes. Tier 2: `tests/integration/end_to_end_smoke_test.py` passes in 17s on the cache. Tier 3: `kentro-server seed-demo` ingests 8 corpus markdown files via real `/documents` route end-to-end (26 entities total). Tier 3.5: kill+restart uvicorn on the same `kentro_state/` — entities, rules (version=2), and tenants config all survive. Tier 4 (real MCP client through `mcp inspector`) deferred — needs the user to install. IMPLEMENTATION_PLAN.md updated with a per-tier status table.
- 2026-05-03 | review-fix | **Fix-pass for Codex + independent reviews of PR #12.** Closed all 20 findings (2 Critical, 4 High, 8 Medium, 6 Low) — see PR #12 review comment for the original list. Highlights: (1) **Critical** auth-bypass closed: `is_admin` added to `AgentConfig`/`Principal`; new `current_admin_principal` (HTTP) and `_require_admin` (MCP) gate `POST /rules/apply`, `POST /schema/register`, `DELETE /documents/{id}`, and the matching MCP tools. Default `local` tenant has `ingestion_agent` as admin and `sales`/`customer_service` as non-admin so the demo's Sales-vs-CS access boundary is real. (2) **Critical** `write_field` ruleset-version skew closed: drop the caller-supplied `ruleset_version`; load once internally and use the same version for ACL eval and lineage stamp. (3) **High** `EntityVisibilityRule` wired into `core/read.py::read_entity` — denied visibility now returns the entity with every declared field HIDDEN. (4) **High** `/memory/remember` `object_json` double-encode fixed — single `json.dumps`; non-string values roundtrip cleanly. ACL is now evaluated once before the per-field write loop (no wasted "first call always denies"). (5) **High** `nl_intents/SKILL.md` ↔ schema mismatch closed — `NLIntentList.notes` field added; orchestrator surfaces step-1 splitter notes. (6) **Medium** `_NLIntentList`/`_ParsedRule` renamed to `NLIntentList`/`NLIntentItem`/`ParsedRule` (no leading underscore in public `__all__`). (7) **Medium** `parse_nl_to_ruleset` gains `max_intents=20` rate-limit guard. (8) **Medium** all mid-function imports hoisted to module top: `core/write.py`, `core/rules.py`, `skills/factory.py` (Anthropic/Gemini providers — instructor is loaded regardless), `main.py` CLI commands. (9) **Medium** lifespan failure handling: `finally` uses `getattr(..., None)` so a partial setup doesn't mask the real exception with `AttributeError`. (10) **Medium** `_LazyMcpMount` documented as a deliberate, narrow exception to "no singletons" (per-lifespan attach/detach; required by FastMCP's once-only `session_manager.run()`). (11) **Medium** `core/read.py::_decode` now logs before falling back on `JSONDecodeError`. (12) **Low** `_validate_rule_against_world` rewritten with `match`/`case` (no more `getattr` after isinstance). (13) **Low** test fakes deduplicated — `tests/unit/_helpers.py` holds the canonical `FakeLLM`, `ADMIN_KEY`, `AGENT_KEY`; `conftest.py` exports the `isolated_state` and `tenants_json_with_admin` fixtures. (14) **Low** `cli_test` URL parsing uses `urlparse`. (15) **Low** boot-time guardrail: `KENTRO_PROD_MODE=true` refuses to start if `tenants.json` still contains any default `local-*-do-not-share` demo key; dev mode logs a warning. (16) **Low** `Note.object_json` carries a `TODO(v0.1)` documenting the type_str/value-type gap. New tests: 7 added (admin gates × 4, entity-visibility hide × 1, object_json roundtrip × 1, PD-before-loop × 1) for **116/116 unit tests passing**; ruff + format + ty all clean.
- 2026-05-03 | step-7 | **Step 7 done — HTTP API + MCP + demo CLI + unstructured memory.** Phases C through G shipped on top of A (foundation cleanup) and B (orchestrators + schema evolution). New surface: `kentro_server.api.{auth,deps,dtos,routes/}` with five domain routers (`documents`, `entities`, `rules`, `schema`, `memory`) wired into `main.py` via `include_router`; per-(tenant, agent) Bearer auth resolves to a typed `Principal` via `TenantRegistry.by_api_key`. `skills/nl_to_ruleset.py::parse_nl_to_ruleset(...)` runs the two-step NL parse with per-intent schema/agent validation and returns the partial-success `NLResponse`. MCP server (`mcp_server.py`) mounts at `/mcp` via `app.mount(streamable_http_app)` wrapped in an `AuthMiddleware` that threads principal + LLM + registry through a `ContextVar`; nine `kentro_*` tools mirror the most-used HTTP routes. `_LazyMcpMount` builds a fresh `FastMCP` per lifespan cycle so test re-entry doesn't trip the once-only session manager. `kentro_server.demo.schemas` is the single source of truth for `Customer` / `Person` (used by the smoke test and the new CLI). Three new CLI commands: `seed-demo` (register schemas + ingest the corpus), `reset-tenant` (wipe on-disk state), `smoke-test` (schema → write → read round-trip via real HTTP). New tests: `api_smoke_test.py` (9), `mcp_smoke_test.py` (5), `cli_test.py` (2), `nl_to_ruleset_test.py` (6) — 22 new tests for a total of **109/109 unit tests passing; ruff + format + ty clean**. Closes Codex's three review findings (critical NL seam, high cache invalidation, medium NLResponse contract drift).
- 2026-05-03 | refactor | **Inverted the LLM cache layering — composition + DI replaces per-provider subclassing.** Codex flagged that `CachingLLMClient` could return stale answers after a `SKILL.md` edit because the cache key did not include the rendered system prompt; the prompt was built *inside* `AnthropicLLMClient` / `GeminiLLMClient`, below the cache wrapper. Fix is structural: introduced `Provider` ABC (`skills/provider.py`) with a single low-level `complete(model, system, user, response_model, …)` method; concrete `AnthropicProvider` / `GeminiProvider` know nothing about skills. `CachingProvider` wraps any `Provider` and fingerprints `(model, system, user, response_class, max_tokens, max_retries)` — the rendered system prompt is always part of the key, so editing `skills/<name>/SKILL.md` invalidates the cache by construction. `LLMClient` stays as the high-level skill ABC; new `DefaultLLMClient` composes two `Provider`s + tier model names via constructor DI, owns the prompt-formatting helpers, and is what `make_llm_client(settings)` returns. `RoutingLLMClient` retired — mixed-tier deployments fall out of composition (pass two different providers). `OfflineLLMClient` retained at the skill layer for tests that want graceful UNRESOLVED behavior; new `OfflineProvider` raises at the low layer. `/llm/stats` now uses `cache_stats(client)` / `cache_metadata(client)` aggregator helpers in `skills/factory.py` that walk the providers and de-dup the shared-instance case. Soft fix for Codex's medium finding: `NLResponse` docstring no longer claims `POST /rules/parse` is implemented (route lands in Phase D). New tests: `test_different_system_prompts_produce_different_cache_keys` (cache layer), `tests/unit/skill_edit_invalidation_test.py` (full DefaultLLMClient → CachingProvider → fake Provider, mutates real `SKILL.md` between calls). Updated existing test fakes (`_FakeOnlineLLM`, `_FakeExtractor`) to satisfy the expanded `LLMClient` ABC (now includes `identify_nl_intents` / `parse_nl_rule` for the in-progress Phase C). Deleted `anthropic_client.py` / `gemini_client.py`. CLAUDE.md gains a new "Dependency injection &amp; composition over inheritance" section that explains the layering and codifies the user's preference. **87/87 unit tests pass; ruff + format + ty clean.**
- 2026-05-03 | review-pass | **Fix-pass for the parallel Codex / `/review` adversarial reviews.** Closed all HIGH findings: (1) added the `kentro_tenants_json` field to `Settings` that the lifespan was reading — server now actually boots; (2) renamed `_RoutingLLMClient` → `RoutingLLMClient`, fixed its `extract_entities` kwarg from `registered_entity_types` to `registered_schemas`, exposed `fast_model`/`smart_model` for stable cache keys; (3) ingestion now rollback-safe (extraction or DB failure deletes the staged blob); (4) `TenantsConfig` validates unique IDs and unique API keys at load time so duplicate keys can no longer silently overwrite the routing table. Also: deleted `.env~` editor backup; `.gitignore` adds `*~` and `.*~`; smoke-test path-relative skip-gate; `kentro/__init__.py` re-exports the user-facing surface so `kentro.Entity` works; `pydantic_core` → `pydantic.fields.PydanticUndefined`; `packages/kentro_server/README.md` aligned with implemented CLI commands; settings values moved to `kentro.toml` (loaded via `TomlConfigSettingsSource`); source removal extracted into `core/source_removal.py::remove_document` (smoke test now uses it). New tests: lifespan boot via `with TestClient(app)`, RoutingLLMClient skill+extract dispatch, inner-model exposure, tenant config validation. CLAUDE.md adds: never `raise Exception(...)`, never `except Exception:`, never nested try/except — and a section documenting the local pre-commit hook. Local pre-commit hook at `scripts/git-hooks/pre-commit` runs ruff (lint + format), ty, types parity, and pytest in ~5s. **80/80 unit tests + smoke test pass; ruff and ty clean.**
- 2026-05-03 | gap-fill | **Big PR — gaps closed end-to-end.** Tenancy formalized via `<repo>/tenants.json` (Pydantic `TenantsConfig`); `TenantRegistry.from_paths(state_dir, config_path)` loads at app startup and auto-creates a default `local` tenant on first run; per-tenant on-disk layout (`state.sqlite`, `docs/`, `witchcraft/`) under `<state_dir>/<tenant_id>/`. Schema registry (`SchemaRegistry`) per tenant, backed by new `SchemaTypeRow` table; SDK ships `EntityTypeDef`/`FieldDef` types and `kentro.schema.entity_type_def_from(cls)` introspects a Pydantic Entity subclass into wire form. Extractor now takes `registered_schemas: list[EntityTypeDef]` and the LLM prompt includes field declarations so it returns canonical field names with values matching the declared types (numbers for `float`, etc.). Synthetic corpus (8 markdown files) generated via real Anthropic into `examples/synthetic_corpus/` (committed). End-to-end smoke test (`tests/integration/end_to_end_smoke_test.py`, gated on `ANTHROPIC_API_KEY`, skipped in CI) walks the full demo: register schemas → ingest call → ingest email → assert conflict → resolve via SkillResolver → source-delete email → fall back to $250K. **Codex's 3 high-severity findings closed**: tenant ID validation + path containment, blob store uses `Path.is_relative_to` and rejects absolute keys, `EntityRow` has `UNIQUE(type, key)` with race-safe `IntegrityError` retry. **76/76 unit tests pass + smoke test green.** CLAUDE.md adds "Run tests before opening or updating a PR" rule. IMPLEMENTATION_PLAN.md gets a "Deferred to the very end" section listing CI as deferred.
- 2026-05-03 | tech-debt | Retired the two module-level singletons (get_settings, get_llm_client) per the No-Singletons rule. Replaced with FastAPI lifespan handler attaching Settings, LLMClient, StoreRegistry to app.state, and Annotated[T, Depends(...)] dependency injection on routes. CLI now constructs Settings() directly. 63/63 unit tests still pass.
- 2026-05-03 | step-6 | **Step 6 done.** Ingestion + extraction + LLM infrastructure. New modules: `settings.py` (pydantic-settings), `skills/{llm_client,anthropic_client,gemini_client,cache,factory}.py`, `extraction/ingestor.py`. `make_llm_client(settings)` detects provider per tier from model prefix (`claude-*`/`gemini-*`), wraps in `CachingLLMClient` (disk-backed at `<state_dir>/.llm_cache/`, hit/miss counters, toggle via `KENTRO_LLM_CACHE_ENABLED`). New CLI: `kentro-server llm-stats` (queries `/llm/stats` on a running server). `ingest_document` runs smart-tier extraction, persists Document + ExtractionStep rows, get-or-creates entities by strict (type, key), records writes via Step 5's `record_field_write` so conflicts and corroboration land naturally. Tests: 10 factory + 6 cache + 4 ingestor = 20 new. **63/63 unit tests pass.** `.env.example` rewritten Anthropic-only with undated model IDs (`claude-haiku-4-5`, `claude-sonnet-4-6`); `KENTRO_LLM_OFFLINE` removed (cache serves the same role). Token telemetry deferred to v0.1.
- 2026-05-03 | decision | **`KENTRO_LLM_OFFLINE` retired.** The flag was an "always-fail-without-fixture" kill-switch I invented for CI. Replaced by the disk cache: hits short-circuit; misses go to the provider when an API key is set; misses raise `LLMConfigError` when no key is set. Cleaner mental model, fewer knobs.
- 2026-05-03 | decision | **Provider dispatch by model prefix.** Factory inspects `KENTRO_LLM_FAST_MODEL` and `KENTRO_LLM_SMART_MODEL`: `claude-*` → AnthropicLLMClient (needs `ANTHROPIC_API_KEY`), `gemini-*` → GeminiLLMClient (needs `GOOGLE_API_KEY`). Mixed providers per tier supported via internal `_RoutingLLMClient`. Misconfiguration raises at startup — never silently falls back.
- 2026-05-03 | step-5 | **Step 5 done.** Server-side conflict detection (`core/conflict.py::record_field_write`) opens a `ConflictRow` only when live writes for the same (entity, field) carry distinct `value_json` — same-value writes are corroboration, not conflict. Read-time resolver dispatch (`core/resolve.py::resolve`) returns a `ResolvedFieldValue` with KNOWN/UNRESOLVED status; AutoResolver dispatches via the active `ConflictRule` and falls back to `LatestWriteResolver` per `demo.md` cell 12. LLMClient seam (`skills/llm_client.py`) lands as ABC + OfflineLLMClient stub + `SkillResolverDecision` Pydantic; Step 6 will plug in Gemini/Anthropic. SDK gains 5 wrapper classes in `kentro/resolvers.py`. Tests: 7 conflict + 12 resolve cases. 43/43 unit tests pass. `superseded` remains untouched in v0; resolution recomputes against live writes on every read so source removal needs no "undo resolution" logic.
- 2026-05-03 | step-4 | **Step 4 done.** Wrote the ACL evaluator at `packages/kentro_server/src/kentro_server/core/acl.py`: three pure functions (`evaluate_field_read`, `evaluate_entity_visibility`, `evaluate_write`) that consume a `RuleSet` + agent/entity/field and return an `AclDecision` (`allowed: bool`, `reason: str | None`). Policy: deny overrides allow; default deny when no rule matches; `requires_approval=True` acts as a deny in v0 with the demo-specified reason `"write blocked: manager approval required"`. 13 new unit tests in `tests/unit/acl_test.py` drive the demo.md access matrix in both initial and post-Scene-4 states. 24/24 unit tests pass.
- 2026-05-03 | step-3 | **Step 3 done.** Built the persistence layer: 8 SQLModel tables (`AgentRow`, `DocumentRow`, `EntityRow`, `FieldWriteRow`, `ConflictRow`, `RuleVersionRow`, `RuleRow`, `ExtractionStepRow`) at `packages/kentro_server/src/kentro_server/store/models.py`; per-tenant `TenantStore` and `StoreRegistry` at `store/tenant_store.py` (each tenant gets its own SQLite engine + docs/witchcraft subdirs); `BlobStore` ABC and `FilesystemBlobStore` at `store/blobs.py` (with path-escape guard). 6 new unit tests cover schema creation, round-trip, blob ops, path-escape rejection, two-tenant isolation, and reset-then-recreate cleanliness. 11/11 unit tests pass. Tenants remain config (no tenant table) per handoff §1.7. Conflict-as-memory: every write persists; `superseded` flags loser rows so source churn can re-resolve.
- 2026-05-03 | step-2 | **Step 2 done.** Wrote Pydantic v2 types covering every DTO the handoff lists: status enums (`FieldStatus`, `WriteStatus`), discriminated-union resolver specs and rules, `RuleSet`, lineage/value carriers (`LineageRecord`, `FieldValue`, `FieldValueCandidate`, `Conflict`), `EntityRecord`, `Agent`, operation results (`WriteResult`, `IngestionResult`, `ExtractionStep`, `ReevaluationReport`, `NLResponse`), and the `Entity` base class. SDK at `packages/kentro/src/kentro/types.py`, server mirror at `packages/kentro_server/src/kentro_server/api/types.py`. Parity test `tests/unit/types_parity_test.py` enforces lockstep across symbols, enum members, and model field shapes (annotation reprs are normalized for the two module prefixes). Sync-types skill at `.claude/skills/sync-types/SKILL.md` documents how to keep the two files coherent and how to record intentional divergence. All 5 unit tests pass.
- 2026-05-03 | scaffold | **Step 0 done.** Built the uv workspace skeleton: root `pyproject.toml` (workspace + dev deps + ruff/pytest config), `.python-version` (3.13), `packages/kentro/` (SDK shell, hatchling), `packages/kentro_server/` (engine shell with typer CLI and FastAPI `/healthz`, hatchling), `packages/kentro_demo_ui/` (placeholder for Step 10), `tests/{unit,integration,scenario}/` skeleton with `tests/unit/healthz_test.py` (passes). `uv sync` generates `uv.lock`; `uv run kentro-server version` works.

## 2026-05-02

- 2026-05-02 | research | Verified Python ↔ Witchcraft integration via subprocess on Mac. Wrote `scripts/python_integration_test.py` in the witchcraft sibling repo: 6-doc kentro-shaped corpus, full `readcsv → embed → index → query/hybrid` chain via `subprocess.run`, parser handles Witchcraft's `{score}: {body}\n @ {sub_idx}` block format (body can span multiple lines, blocks separated by `===`). End-to-end: 13ms readcsv + 147ms embed + 18ms index for 6 docs; ~110ms per query (subprocess startup + model load + search). Semantic queries land correct top-k for keyword-aligned questions ("what is the renewal price for Acme" → meeting transcript at 0.86). Two recall caveats observed: T5 weakly embeds bare digit strings (phone-number lookup missed); short docs make the hardcoded 0.7 threshold prune some borderline matches. Both are corpus-quality issues, not integration issues. **Conclusion: Witchcraft + Python via subprocess is a viable v0 integration path.**
- 2026-05-02 | decision | Locked v0 Witchcraft integration approach: **subprocess wrapper around `warp-cli`**. Per-doc `add` via append-to-tenant-TSV + `readcsv → embed → index` (incremental — embed/index only touch pending rows). Per-doc `remove` for v0 via wipe-and-re-ingest (acceptable at the demo's 6-docs-per-tenant scale, ~200ms). PyO3 binding (`kentro-witchcraft` crate via `maturin`) deferred to v0.1 if scale demands it. Rationale: zero new toolchain in the kentro build, works today on every platform Witchcraft builds on, fits comfortably within demo latency budgets. Handoff not changed — Witchcraft commitment unaffected; this is implementation detail.
- 2026-05-02 | research | Validated Dropbox Witchcraft (`github.com/dropbox/witchcraft` @ master) end-to-end on Mac arm64 (M2 Max, Metal). Build chain ran cleanly via `make warp-cli` after installing rustup (rustc 1.95.0). Footprint: HF download 422 MB (`xtr-base-en/`) + 210 MB (`xtr.safetensors`) + 62 MB (`assets/` GGUF + tokenizer/config) + 1.3 GB (`target/` build dir) + 828 MB (Python venv). Persistent runtime: 62 MB assets + binary. Indexed nfcorpus (3633 medical-research docs): `readcsv` 0.7s, `embed` 1m25s, `index` 10.7s, `mydb.sqlite` ends at 181 MB. Semantic `query` and `hybrid` both return correct top-k for medical queries (e.g. "milk intake / acne" returns the milk-acne association abstracts at scores 0.92/0.91/0.91). Subprocess wall-time per query: ~140ms steady-state (~270ms cold). README's 21ms p95 is the in-process latency without subprocess startup. CLI surface confirmed: `clear | readcsv | embed | index | reindex | query | hybrid | querycsv | hybridcsv | fulltextcsv | score`. Output format is plain text `{score}: {body} @ {sub_idx}` with `===` separators — no JSON flag. DB path hardcoded to `./mydb.sqlite`, assets to `./assets/`.
- 2026-05-02 | research | Mapped Witchcraft Rust library API (`src/lib.rs` `pub use`s + `src/db.rs` `impl DB`). `DB` exposes `add_doc`, `add_docs_batch`, `remove_doc(uuid)`, `delete_with_filter`, `clear`, `begin/commit/rollback_transaction`. Library functions: `embed_chunks(db, embedder, limit)`, `index_chunks(db, device)`, `search(...)`, `fulltext_search(...)`, `reciprocal_rank_fusion(...)`. Per-doc add/remove is supported by the Rust library — but `warp-cli` only exposes the batch pipeline. Per-doc churn from Python requires PyO3 bindings, the Node native module, or a long-running custom Rust daemon.
- 2026-05-02 | revert | Reverted unauthorized Witchcraft-related edits to `implementation-handoff.md`. Restored §1.3 Semantic-index row (Witchcraft) and Semantic-index-fallback row (BM25 fallback) to their `origin/main` text. Restored "Witchcraft" word in §1.7 persistent-state bullet. Removed BM25/Witchcraft post-handoff note and BM25-comparator open question from `IMPLEMENTATION_PLAN.md`; replaced with the Witchcraft local-validation experiment ask. Witchcraft is the v0 semantic index per the handoff; integration approach to be decided after the validation experiment.
- 2026-05-02 | docs | Added a "handoff is ground truth — do not diverge without explicit approval" section to `CLAUDE.md`. Six hard rules: no inventing APIs, no silent library substitution, no skipped/merged steps, no unilateral "improvements," flag ambiguities instead of guessing, surface deferred decisions and wait. Approved divergences must be recorded in changelog + plan + handoff.
- 2026-05-02 | docs | Added `.env.example` with placeholder values for LLM providers, server settings, and demo tenancy. Created empty `.env`. Added `.gitignore` covering Python, env files, editor noise, demo-UI artifacts, and local state.
- 2026-05-02 | docs | Seeded `IMPLEMENTATION_PLAN.md` (live plan mirroring the 11 handoff steps + a Step 0 scaffold and a Step 12 GCP-deploy slot at the end).
- 2026-05-02 | docs | Seeded `CHANGE_LOG.md` (this file).
- 2026-05-02 | docs | Updated `CLAUDE.md` with explicit instructions to keep `IMPLEMENTATION_PLAN.md` and `CHANGE_LOG.md` current; both must move together when a step finishes.
- 2026-05-02 | research | Validated Witchcraft. **Two unrelated projects share the name.** PyPI `witchcraft` 0.3.9 is an unrelated personal SQL ORM by Peter Facka (`spookyowl/witchcraft`); fails on Python 3.13. The Witchcraft the handoff actually meant is `github.com/dropbox/witchcraft` — Dropbox's Rust reimplementation of XTR-WARP over single-file SQLite, real and matching the handoff's promise (~21ms p95) but **not on PyPI**: ships as a Rust binary + Node native module, no Python wheel. The `jlscheerer/xtr-warp` research engine is conda-only and GPU-required for indexing. Conclusion: for v0, baseline on BM25 (`rank_bm25`). Dropbox Witchcraft via subprocess remains an option if we need the quality, but it adds a Rust toolchain to the build. Comparator for the eval script pending user choice.
