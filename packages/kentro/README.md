# kentro

Thin Python SDK for [kentro-server](../kentro_server). Two clients (`AdminClient`, `AgentClient`), Pydantic types, Jupyter/Colab visualization helpers. No persistence, no extraction, no LLM calls — every public method is a server call.

See `implementation-handoff.md` at the repo root for the full architecture.
