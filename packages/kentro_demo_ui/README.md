# kentro_demo_ui

Demo web UI placeholder. Built in Step 10 — see `implementation-handoff.md` Step 10.

The UI ships as a static build that `kentro-server` serves via FastAPI `StaticFiles`. No separate frontend deployment.
